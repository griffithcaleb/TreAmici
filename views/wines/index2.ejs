<% layout('layouts/boilerplate') %>
<link rel="stylesheet" href="/stylesheets/index.css">
<!-- <div class="container"> -->
    <div class="row">
      <div class="col-sm">
        <!-- <div class="row"> -->
        <h1 id="italy-wines" class="col">Italy,
            Wines
        </h1>
                <!-- </div> -->
        <a id="adding" href="/wines/new" class="col btn btn-outline-secondary"">Add Wine</a>
        <form id="search-wine" class="col input-group">
            <input type="text" class="form-control" placeholder="Search Wines..." aria-label="Recipient's username" aria-describedby="button-addon2">
            <button class="btn btn-outline-secondary " type="button" id="button-addon2">Search</button>
        </form>
      </div>
      <div id="map" class="col-sm"></div>
    <div>
        <% for (let wine of wines) {%>
            <div class="card md-3 info">
                <div class="row">
                    <div class="col-md-4">
                        <% if(wine.images.length)  {%> 
                        <img class="img-fluid" alt="" src="<%=wine.images[0].url%>">
                        <% }else {%> 
                            <img class="img-fluid" alt="" src="https://res.cloudinary.com/dmljm2vic/image/upload/v1611628550/YelpCamp/bbqlonquv3wgpsfainfy.jpg">
                            <% } %> 
                    </div>
                    <div id="mine" class="col-md-8">
                        <ul id="me" class="card-body" style="display: block">
                            <h5 class="card-title info"><%= wine.title %></h5>
                            <p class="card-text info"><%= wine.description %></p>
                            <p class="card-text">
                                <small class="text-muted info"><%= wine.location%></small>
                            </p>
                            <a class="btn btn-primary info" href="/wines/<%=wine._id%>">View <%=wine.title%></a>
                        </ul>
                        </div>
                    </div>
                </div>
            </div>
        <!-- </div> -->
            <% } %>
        </ul>
<script>
    const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
    const wines = { features: <%- JSON.stringify(wines) %>}
       </script>

<script src="/javascripts/clusterMap.js"></script>
<script src="/javascripts/search.js"></script>

